# Copy to .env for local development.
# Only VITE_-prefixed variables reach the browser, and none of them may be secrets.

# Local FastAPI. In production this is the Railway API origin, e.g.
# https://api.vegasthisweekend.com
VITE_API_BASE_URL=http://127.0.0.1:8000
