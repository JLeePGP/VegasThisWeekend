[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"

# Client-side routing: /s/<token> and /saved are React Router paths, not files.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
